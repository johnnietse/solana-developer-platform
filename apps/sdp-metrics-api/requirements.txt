flask>=3.0,<4.0
gunicorn>=22.0,<24.0
polars>=1.9,<2.0
# pl.read_delta shells out to delta-rs; not a transitive dep of polars.
# Version range must match what polars declares for its "deltalake" extra.
deltalake>=1.0,!=1.5.*
# Not imported directly; pl.CredentialProviderAWS lazily imports it for AWS_PROFILE resolution.
boto3>=1.34,<2.0
